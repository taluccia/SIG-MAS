---
title: "Veg Area Grids"
author: "Anna Talucci"
date: "8/23/2023"
output: html_document
---



# Packages
```{r}
library(tidyverse)
library(sf)
library(viridis)
```

# Data
## Tx area


```{r}
ccTx = read.csv("../data/VegGrid/ccGridTx.csv")
scTx = read.csv("../data/VegGrid/scGridTx.csv")
```


```{r}
ccHucSW = read.csv("../data/VegGrid/ccTxHucSw.csv")
scHucSW = read.csv("../data/VegGrid/scTxHucSw.csv")
```

```{r}
ccGridSw = read.csv("../data/VegGrid/ccTxGridSw.csv")
scGridSw = read.csv("../data/VegGrid/scTxGridSw.csv")
```

```{r}
head(ccTx)
head(scTx)
```

```{r}
head(ccHucs)
head(scHucs)
```
# Area total

```{r}
cc %>% summarise(totalSqm = sum(vegSum),
                 totalSqkm = (totalSqm/1e6),
                 toalAcres = (totalSqkm*247.1))
```

```{r}
ccPx %>% summarise(totalSqm = sum(sum),
                 totalSqkm = (totalSqm/1e6),
                 toalAcres = (totalSqkm*247.1))
```
```{r}
sc %>% summarise(totalSqm = sum(vegSum),
                 totalSqkm = (totalSqm/1000000),
                 toalAcres = (totalSqkm*247.1))
```

```{r}
ccTx %>% summarise(total = sum(treatAcres))

```

```{r}
scTx %>% summarise(total = sum(treatAcres))

```

```{r}
ccHucs %>% summarise(total = sum(treatAcres))
scHucs %>% summarise(total = sum(treatAcres))
```

# Softwood summarize

```{r}
( ccHucSW1 = ccHucSW %>% summarise(total = sum(treatAcres)) )
( scHucSW1 = scHucSW %>% summarise(total = sum(treatAcres)) )
```

```{r}
( ccGridSw1 = ccGridSw %>% summarise(total = sum(treatAcres)) )
( scGridSw1 = scGridSw %>% summarise(total = sum(treatAcres)) )
```

## Softwood outside grids
```{r}
ccHucSW1-ccGridSw1
```

```{r}
scHucSW1-scGridSw1
```